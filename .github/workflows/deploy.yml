name: CI/CD Deploy Docker App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH va Deploy app len server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22  # Đổi từ 24700 sang 22
          script: |
            cd /var/www/STUDY-ONLINE
            git pull origin main
            docker compose down
            docker compose up -d --build
            docker compose ps
            docker compose logs --tail=50
            curl -I https://ledung.cloud
            curl -k https://ledung.cloud







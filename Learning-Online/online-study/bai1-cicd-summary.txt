1. Thông tin Repository:
   - Repo URL: https://github.com/tiendung209/STUDY-ONLINE
   - Branch: main

2. Tóm tắt luồng CI/CD Workflow:
   - Tự động kích hoạt (trigger) khi có commit mới được push lên nhánh main.
   - Kết nối SSH an toàn tới Server IP (202.92.7.123) qua port 24700 bằng SSH Private Key (lưu trong GitHub Secrets).
   - Tự động thực thi các lệnh trên Server:
     + cd /var/www/STUDY-ONLINE
     + git pull origin main
     + docker compose down
     + docker compose up -d --build

3. Trạng thái kiểm tra trên Server (Verification):
   - Container Status: Running (docker compose ps)
   - Application Logs: Khởi động bình thường, không xuất hiện lỗi (docker compose logs)
   - Response / Health check: Trả về phản hồi thành công (curl http://localhost:8080)

4. Kết luận:
   - Quy trình CI/CD hoàn tất, ứng dụng được tự động cập nhật và triển khai thành công lên Server